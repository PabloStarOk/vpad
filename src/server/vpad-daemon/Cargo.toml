[package]
name = "vpad-daemon"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["rt", "macros", "sync", "net"] }
libmdns = "0.10"
log = { version = "0.4.30", features = ["release_max_level_info"] }
env_logger = "0.11.10"
evdev-rs = "0.6.3"
